import { Button as IButton, Text as IText, Icon as IIcon, Variation as IVariation } from "../../../types/AndroidSpec/Button";
export declare class Button implements IButton {
    style: Object;
    background?: string;
    Text?: IText;
    Icon?: IIcon;
    variation?: IVariation;
    constructor();
}
